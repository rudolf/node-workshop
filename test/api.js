assert = require('assert')

describe('api/v1', function() {

  describe('/questions', function() {

    before(function(done){
      //create fixtures for tests
      done()
    })
  
    it('POST creates a new question', function(done){
      done()
    })

    it('GET retrieves a list of unanswered questions', function(done) {
      done()
    })

  })

  describe('/questions/:id', function() {

    before(function(done){
      //create fixtures for tests
      done()
    })

    it('GET retrieves the specified question', function(done){
      done()
    })

    it('PUT updates the specified question', function(done){
      done()
    })

    it('DEL removes a specific question', function(done){
      done()
    })

  })
})
